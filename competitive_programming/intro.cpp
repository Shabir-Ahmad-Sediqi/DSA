#include <bits/stdc++.h>
typedef long long 11;

using namespace std;

int main(){
    // string s;
    // getline(cin, s);
    // cout<<s<<"\n";

    // string text;

    // The following loop is important when user input is unknown

    // while(cin >> text){
    //     // Code
    // }

    // when reading from file and writing the output to another file
    
    // freopen("inpur.txt", "r", stdin);
    // freopen("output.txt", "w", stdout);
}